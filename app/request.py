def get_pickup(pick_id):
    
    pickup_object = None
    
    if response:
        pick_id = response.get('pick_id')
        author = response.get('author')
        pickupLine = response.get('pickupLine')
        
    return pickup_object
    print (pickup_object)